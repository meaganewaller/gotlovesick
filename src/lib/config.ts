const config = {
  siteName: 'secret gardens',
  siteDescription: 'i hate it here so i will go to secret gardens in my mind',
  siteUrl: `${process.env.NEXT_PUBLIC_FRONT_SITE_URL}`
}

export default config
